<% provide(:title, 'All users') %>

<h1>All users</h1>

<div class="row">
  <div class="pagination col-md-8">
    <%= will_paginate %>
    <ul class="users">
        <%= render @users %>
    </ul>
    <%= will_paginate %>
  </div>
</div>
